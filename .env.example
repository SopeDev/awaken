# LLM avatar decisions (/api/decision)
# Copy to .env.local and fill in (never commit .env.local)
OPENAI_API_KEY=
# Use an id from GET /v1/models for this key; must match Project → Limits "Allowed models"
LLM_MODEL=gpt-4.1-mini
USE_LOCAL_LLM=false
OLLAMA_HOST=http://127.0.0.1:11434
# Include LLM debug payload in /api/decision response; Room logs it in the browser console (set false to disable)
LOG_LLM_IO=true
